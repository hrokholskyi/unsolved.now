source "https://rubygems.org"

# Mirrors the exact gem set GitHub Pages uses for branch builds,
# so a local build is a faithful preview of the deployed one.
gem "github-pages", group: :jekyll_plugins

# Jekyll's dev server dependency, no longer bundled with Ruby 3+.
gem "webrick"
